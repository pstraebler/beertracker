services:
  beer-tracker:
    build: .
    ports:
      - "${HOST_PORT}:${APP_PORT}"
    volumes:
      - beertracker_data:/app/data
    env_file:
      - .env
    command: python app.py

volumes:
  beertracker_data:
    driver: local