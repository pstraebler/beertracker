services:
  beer-tracker:
    build: .
    ports:
      - "8080:${APP_PORT}"
    volumes:
      - beertracker_data:/app/data
    env_file:
      - .env
    command: python app.py

volumes:
  beertracker_data:
    driver: local